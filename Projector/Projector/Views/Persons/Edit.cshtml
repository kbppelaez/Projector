@model EditPersonViewModel

@{
    ViewData["Title"] = "Create New Person";
    Layout = "_Authenticated";
}

<div class="flex flex-col w-full">
    @if (Model.Errors.Any())
    {
        <div class="FormError" role="alert">
            <strong class="FormErrorHeader">Errors occured.</strong>
            <ul>
                @foreach (var error in Model.Errors)
                {
                    <li class="FormErrorContent">@error</li>
                }
            </ul>
        </div>
    }

    <div class="flex-grow w-full">
        <form method="POST" asp-controller="Persons" asp-action="Edit">
            <partial name="_CreatePersonInput" model="@Model.EditableValues" />
            <div class="form-group">
                <input type="submit" value="Edit Account" class="Primary FormButton" />
                <a asp-controller="Persons" asp-action="Details" asp-route-personId="@Model.PersonId" class="Neutral FormButton">View Details</a>
            </div>
        </form>
    </div>
</div>
